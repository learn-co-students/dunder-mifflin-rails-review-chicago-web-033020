<p><%= link_to "All Employees", employees_path %> | <%= link_to "Edit Employee", edit_employee_path(@employee)%></p>

<h1>Name: <%= @employee.name %></h1>

<h2>Alias: <%= @employee.alias %></h2>

<h2>Title: <%= @employee.title %></h2>

<h2>Office: <%= @employee.office %></h2>

<h2>Dog: <%= @employee.dog.name %></h2>

<%= image_tag(@employee.img_url, alt: "#{@employee.name}") %>